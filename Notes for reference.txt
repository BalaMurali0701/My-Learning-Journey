a.endsWith(b) //compares if the string a ends with string b --> ( a = bill, b = ill ==> true ) , ( a = cheems, b = dog ==> false ).
a.size() //find number of elements in array, queue
Arrays.sort(arr) //to sort an array arr
